[workspace]
channels = ["conda-forge"]
name = "agents-setup"
platforms = ["osx-arm64"]
version = "0.1.0"

[tasks]
claude = "scripts/start_claude_vm.sh"

[activation]
scripts = ["scripts/check_colima.sh"]

[dependencies]
